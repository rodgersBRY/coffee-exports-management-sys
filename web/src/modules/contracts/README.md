# Contracts Module

## Purpose
- Creates sales contracts, allocates lots, and tracks fulfillment dashboards.

## Files
- `config.ts`: JSON templates for contract creation and lot allocation.

## Notes
- Allocation endpoint requires a dynamic path: `contracts/:contractId/allocations`.
