# Root-level env file — used by docker-compose for variable substitution.
# Copy this to .env.prod and fill in real values before deploying.

# PostgreSQL credentials (shared between postgres service and api DATABASE_URL)
POSTGRES_USER=ceoms_user
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD
POSTGRES_DB=ceoms

# Baked into the Next.js client bundle at build time (optional override)
NEXT_PUBLIC_APP_NAME=CEOMS Web
